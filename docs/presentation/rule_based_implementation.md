# 🎯 규칙기반 자연어 생성 상세 구현

> 발표에서 가장 중요한 핵심 내용입니다. 이 시스템의 핵심 기술력을 보여주는 부분입니다.

---

## 📌 개요: 왜 규칙기반인가?

### 제안서 요구사항
> "설명 문구 작성: 보도자료 내 '▲▲지역 생산은 전년동분기대비 0% 증가' 등 설명 문구 자연어 생성 **규칙과 LLM**을 활용하여 자동 생성"

### 우리의 선택: 규칙기반 우선

| 방식 | 구현 | 이유 |
|------|------|------|
| **규칙기반 자연어 생성** | ✅ 완료 | 정확성, 검증가능성, 일관성 |
| **LLM 활용** | 📋 향후 과제 | 고도화 단계에서 해설/분석 문구에 적용 |

---

## 🔧 규칙기반 시스템의 3계층 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    규칙기반 자연어 생성 시스템                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  Generator  │───▶│   Schema    │───▶│  Template   │     │
│  │   (Python)  │    │   (JSON)    │    │   (HTML)    │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│       │                   │                   │             │
│       ▼                   ▼                   ▼             │
│  데이터 추출 규칙    데이터 구조 정의     텍스트 생성 규칙      │
│  매핑 사전          컬럼 매핑           조건부 문장 생성      │
│  증감률 계산        유효성 검사         포맷팅               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 1️⃣ Generator 계층: 데이터 추출 규칙

### 1.1 매핑 사전 (Mapping Dictionary)

엑셀의 원본 데이터를 보도자료 표기명으로 **일관되게 변환**하는 규칙입니다.

```python
# templates/mining_manufacturing_generator.py

class 광공업생산Generator:
    # 업종명 매핑 사전 (엑셀 데이터 → 보도자료 표기명)
    INDUSTRY_NAME_MAP = {
        "전자 부품, 컴퓨터, 영상, 음향 및 통신장비 제조업": "반도체·전자부품",
        "의료, 정밀, 광학 기기 및 시계 제조업": "의료·정밀",
        "의료용 물질 및 의약품 제조업": "의약품",
        "기타 운송장비 제조업": "기타 운송장비",
        "자동차 및 트레일러 제조업": "자동차·트레일러",
        "전기, 가스, 증기 및 공기 조절 공급업": "전기·가스업",
        "화학 물질 및 화학제품 제조업; 의약품 제외": "화학물질",
        # ... 28개 업종 매핑
    }
    
    # 지역명 표준화 (띄어쓰기 포함 공식 표기)
    REGION_DISPLAY_MAP = {
        "전국": "전 국",
        "서울": "서 울",
        "부산": "부 산",
        "경기": "경 기",
        # ... 17개 시도 매핑
    }
```

**왜 중요한가?**
- 엑셀 원본: "전자 부품, 컴퓨터, 영상, 음향 및 통신장비 제조업" (31자)
- 보도자료 표기: "반도체·전자부품" (7자)
- **일관된 용어 사용**으로 보도자료 품질 보장

---

### 1.2 컬럼 인덱스 매핑

엑셀 시트의 어느 컬럼에서 어떤 데이터를 가져올지 **명시적으로 정의**합니다.

```python
# templates/regional_generator.py

class RegionalGenerator:
    # 분기별 컬럼 매핑 (각 시트별로 다름)
    QUARTER_COLS = {
        'A 분석': {  # 광공업생산 분석 시트
            '2023_1Q': 12, '2023_2Q': 13, '2023_3Q': 14, '2023_4Q': 15,
            '2024_1Q': 16, '2024_2Q': 17, '2024_3Q': 18, '2024_4Q': 19,
            '2025_1Q': 20, '2025_2Q': 21
        },
        'B 분석': {  # 서비스업생산 분석 시트
            '2023_1Q': 11, '2023_2Q': 12, '2023_3Q': 13, '2023_4Q': 14,
            '2024_1Q': 15, '2024_2Q': 16, '2024_3Q': 17, '2024_4Q': 18,
            '2025_1Q': 19, '2025_2Q': 20
        },
        # ... 8개 부문 × 10개 분기 = 80개 매핑
    }
```

**왜 중요한가?**
- 엑셀 시트마다 컬럼 구조가 다름
- 잘못된 컬럼 참조 시 **수치 오류** 발생
- 명시적 매핑으로 **검증 가능성** 확보

---

### 1.3 증감률 계산 규칙

**전년동분기대비 증감률**을 자동 계산하는 규칙입니다.

```python
# templates/mining_manufacturing_generator.py

def _extract_nationwide_from_aggregation(self) -> dict:
    """집계 시트에서 전국 데이터 추출 (증감률 직접 계산)"""
    
    # 당분기(2025.2/4)와 전년동분기(2024.2/4) 지수로 증감률 계산
    current_index = safe_float(nationwide_total[26], 100)  # 2025.2/4p
    prev_year_index = safe_float(nationwide_total[22], 100)  # 2024.2/4
    
    # 증감률 계산 공식
    if prev_year_index and prev_year_index != 0:
        growth_rate = ((current_index - prev_year_index) / prev_year_index) * 100
    else:
        growth_rate = 0.0
    
    # 기여도 계산 (업종별)
    contribution = (curr - prev) / prev_year_index * weight / 10000 * 100
    
    return {
        "growth_rate": round(growth_rate, 1),
        "growth_direction": "증가" if growth_rate > 0 else "감소",  # ⬅️ 자동 판단
        # ...
    }
```

**핵심 규칙:**
```
증감률 = (당기지수 - 전년동분기지수) / 전년동분기지수 × 100
기여도 = (당기지수 - 전년동분기지수) / 전국전년동분기지수 × 가중치/10000 × 100
```

---

### 1.4 순위/정렬 규칙

증가 지역과 감소 지역을 **자동 분류**하고 **순위**를 매깁니다.

```python
# templates/mining_manufacturing_generator.py

def extract_regional_data(self) -> dict:
    """시도별 데이터 추출"""
    
    regions_data = []
    for region in individual_regions:
        # ... 데이터 추출 ...
        regions_data.append({
            "region": region,
            "growth_rate": round(growth_rate, 1),
            "top_industries": sorted_ind[:3]  # 상위 3개 업종
        })
    
    # 🔑 핵심: 증가/감소 지역 자동 분류 및 정렬
    increase_regions = sorted(
        [r for r in regions_data if r["growth_rate"] > 0],
        key=lambda x: x["growth_rate"],
        reverse=True  # 높은 순
    )
    
    decrease_regions = sorted(
        [r for r in regions_data if r["growth_rate"] < 0],
        key=lambda x: x["growth_rate"]  # 낮은 순 (가장 큰 감소가 먼저)
    )
    
    return {
        "increase_regions": increase_regions,
        "decrease_regions": decrease_regions,
        "region_count": len(increase_regions)  # 증가 지역 수
    }
```

**자동화된 판단:**
- `growth_rate > 0` → 증가 지역
- `growth_rate < 0` → 감소 지역
- 정렬: 증가는 큰 순, 감소는 작은 순

---

## 2️⃣ Template 계층: 텍스트 생성 규칙

### 2.1 조건부 문장 생성

**Jinja2 템플릿**을 사용하여 데이터에 따라 **자동으로 문장을 생성**합니다.

```html
<!-- templates/mining_manufacturing_template.html -->

<!-- 요약 박스: 증가/감소에 따른 문장 자동 생성 -->
<div class="summary-box">
    <div class="headline">
        ◆ 광공업생산은 
        {% for region in summary_box.main_increase_regions[:3] %}
            <span class="highlight">{{ region.region }}</span>
            ({{ region.industries | join(', ') }})
            {% if not loop.last %}, {% endif %}
        {% endfor %}
        등 <span class="highlight">{{ summary_box.region_count }}개</span> 시도에서 
        전년동분기대비 <span class="increase">증가</span>
    </div>
    
    <div class="nationwide">
        □ 전국 광공업생산({{ "%.1f"|format(nationwide_data.production_index) }})은 
        {{ nationwide_data.main_increase_industries | map(attribute='name') | join(', ') }} 
        등의 생산이 늘어 전년동분기대비 
        <span class="increase">{{ "%.1f"|format(nationwide_data.growth_rate) }}% 증가</span>
    </div>
    
    <div class="regional">
        ○ {% for region in regional_data.decrease_regions[:3] %}
            <span class="highlight">{{ region.region }}</span>({{ "%.1f"|format(region.growth_rate) }}%)
            {% if not loop.last %}, {% endif %}
        {% endfor %}은 
        {{ regional_data.decrease_regions[0].top_industries | map(attribute='name') | join(', ') }} 
        등의 생산이 줄어 <span class="decrease">감소</span>하였으나, 
        
        {% for region in regional_data.increase_regions[:3] %}
            <span class="highlight">{{ region.region }}</span>({{ "%.1f"|format(region.growth_rate) }}%)
            {% if not loop.last %}, {% endif %}
        {% endfor %}은 
        {{ regional_data.increase_regions[0].top_industries | map(attribute='name') | join(', ') }} 
        등의 생산이 늘어 <span class="increase">증가</span>
    </div>
</div>
```

**생성 결과 예시:**
```
◆ 광공업생산은 충남(반도체·전자부품, 자동차·트레일러), 경북(의약품, 기타기계장비), 
  충북(반도체·전자부품, 의약품) 등 11개 시도에서 전년동분기대비 증가

□ 전국 광공업생산(108.2)은 반도체·전자부품, 자동차·트레일러, 의약품 등의 
  생산이 늘어 전년동분기대비 3.2% 증가

○ 울산(-5.2%), 경남(-3.8%), 전남(-2.1%)은 석유정제품, 1차금속, 화학물질 등의 
  생산이 줄어 감소하였으나, 충남(8.5%), 경북(7.2%), 충북(6.1%)은 
  반도체·전자부품, 의약품 등의 생산이 늘어 증가
```

---

### 2.2 증감 방향 자동 판단

값의 부호에 따라 **"증가" / "감소"를 자동 선택**합니다.

```html
<!-- 방법 1: 삼항 연산자 스타일 -->
<span class="{% if value >= 0 %}increase{% else %}decrease{% endif %}">
    {{ "%.1f"|format(value) }}% 
    {% if value >= 0 %}증가{% else %}감소{% endif %}
</span>

<!-- 방법 2: 화살표 기호 사용 -->
<span class="{% if indicator.nationwide_change >= 0 %}increase{% else %}decrease{% endif %}">
    {{ indicator.nationwide_value }}
    {% if indicator.nationwide_change >= 0 %}▲{% else %}▼{% endif %}
</span>

<!-- 방법 3: 물가 지표 (상승/하락) -->
<span class="{% if cpi.change > 0 %}increase{% else %}decrease{% endif %}">
    {{ "%.1f"|format(cpi.change) }}%p 
    {% if cpi.change > 0 %}상승{% else %}하락{% endif %}
</span>
```

**규칙 테이블:**

| 조건 | CSS 클래스 | 표시 텍스트 |
|------|-----------|-----------|
| `value > 0` | `.increase` | "증가" / "▲" |
| `value < 0` | `.decrease` | "감소" / "▼" |
| `value == 0` | `.same` | "보합" / "-" |

---

### 2.3 반복 패턴 자동화

**for 루프**를 사용하여 반복되는 패턴을 자동 생성합니다.

```html
<!-- 주요 증감 지역 및 업종 테이블 -->
<div class="detail-box">
    <div class="title">&lt; 주요 증감 지역 및 업종 &gt;</div>
    
    <!-- 전국 데이터 -->
    <div class="item">
        ·전 국( {{ "%.1f"|format(nationwide_data.growth_rate) }}%): 
        {% for industry in nationwide_data.main_increase_industries[:3] %}
            {{ industry.name }}({{ "%.1f"|format(industry.growth_rate) }}%)
            {% if not loop.last %}, {% endif %}
        {% endfor %}
    </div>
    
    <div class="separator"></div>
    
    <!-- 증가 지역 Top 3 (자동 반복) -->
    {% for region in top3_increase_regions %}
    <div class="item">
        ·{{ region.region }}( {{ "%.1f"|format(region.growth_rate) }}%): 
        {% for industry in region.industries[:3] %}
            {{ industry.name }}({{ "%.1f"|format(industry.growth_rate) }}%)
            {% if not loop.last %}, {% endif %}
        {% endfor %}
    </div>
    {% endfor %}
    
    <div class="separator"></div>
    
    <!-- 감소 지역 Top 3 (자동 반복) -->
    {% for region in top3_decrease_regions %}
    <div class="item">
        ·{{ region.region }}({{ "%.1f"|format(region.growth_rate) }}%): 
        {% for industry in region.industries[:3] %}
            {{ industry.name }}({{ "%.1f"|format(industry.growth_rate) }}%)
            {% if not loop.last %}, {% endif %}
        {% endfor %}
    </div>
    {% endfor %}
</div>
```

---

### 2.4 결측치 처리 규칙

데이터가 없는 경우 **노란색 플레이스홀더**를 표시합니다.

```html
<!-- 결측치 플레이스홀더 매크로 정의 -->
{% macro val(value, format="%.1f", suffix="") %}
    {%- if value is none or value == '' or value == '-' or value == 'N/A' -%}
        <span class="editable-placeholder" contenteditable="true">[  ]</span>{{ suffix }}
    {%- else -%}
        {{ format|format(value) }}{{ suffix }}
    {%- endif -%}
{% endmacro %}

<!-- 사용 예시 -->
<td>{{ val(rate) }}</td>
<td>{{ val(index, "%.1f") }}</td>
```

**CSS 스타일:**
```css
.editable-placeholder {
    background-color: #fff59d;  /* 노란색 배경 */
    padding: 0 4px;
    border: 1px dashed #fbc02d;
    color: #5d4037;
    display: inline-block;
    min-width: 25px;
    text-align: center;
}
```

---

## 3️⃣ Schema 계층: 데이터 구조 정의

### 3.1 JSON Schema 구조

각 보도자료의 **데이터 구조**를 JSON으로 정의합니다.

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "광공업생산 보도자료 데이터 스키마",
  "type": "object",
  "required": ["report_info", "nationwide_data", "regional_data"],
  
  "properties": {
    "report_info": {
      "type": "object",
      "properties": {
        "year": {"type": "integer", "example": 2025},
        "quarter": {"type": "integer", "example": 2},
        "data_source": {"type": "string"}
      }
    },
    
    "nationwide_data": {
      "type": "object",
      "properties": {
        "production_index": {"type": "number", "example": 108.2},
        "growth_rate": {"type": "number", "example": 3.2},
        "growth_direction": {"type": "string", "enum": ["증가", "감소"]}
      }
    },
    
    "regional_data": {
      "type": "object",
      "properties": {
        "increase_regions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "region": {"type": "string"},
              "growth_rate": {"type": "number"},
              "top_industries": {"type": "array"}
            }
          }
        }
      }
    }
  },
  
  "excel_column_mapping": {
    "지역이름": 4,
    "분류단계": 5,
    "산업코드": 7,
    "산업이름": 8,
    "당분기지수": 26,
    "전년동분기지수": 22,
    "기여도": 28
  }
}
```

---

## 4️⃣ 전체 데이터 흐름

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         규칙기반 자연어 생성 흐름                          │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [엑셀 데이터]                                                            │
│       │                                                                  │
│       ▼                                                                  │
│  ┌─────────────────┐                                                     │
│  │   Generator     │  ① 매핑 규칙: 업종명/지역명 표준화                    │
│  │   (Python)      │  ② 계산 규칙: 증감률, 기여도 자동 계산               │
│  │                 │  ③ 정렬 규칙: 증가/감소 분류, 순위 결정              │
│  └────────┬────────┘                                                     │
│           │                                                              │
│           ▼                                                              │
│  ┌─────────────────┐                                                     │
│  │  Data Dict      │  {                                                  │
│  │  (Python Dict)  │    "nationwide_data": {"growth_rate": 3.2, ...},    │
│  │                 │    "regional_data": {"increase_regions": [...]}     │
│  └────────┬────────┘  }                                                  │
│           │                                                              │
│           ▼                                                              │
│  ┌─────────────────┐                                                     │
│  │   Template      │  ④ 조건 규칙: 값에 따라 "증가"/"감소" 선택           │
│  │   (Jinja2)      │  ⑤ 포맷 규칙: 소수점, 단위 등 표시 형식              │
│  │                 │  ⑥ 반복 규칙: 리스트 데이터 반복 출력                │
│  └────────┬────────┘                                                     │
│           │                                                              │
│           ▼                                                              │
│  ┌─────────────────┐                                                     │
│  │   HTML 보도자료  │  완성된 자연어 문장                                  │
│  │                 │  "충남(8.5%)은 반도체·전자부품 등의 생산이 늘어 증가" │
│  └─────────────────┘                                                     │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 5️⃣ LLM과의 비교

### 왜 규칙기반을 선택했는가?

| 기준 | 규칙기반 | LLM |
|------|---------|-----|
| **정확성** | ✅ 100% (데이터가 맞으면 결과도 맞음) | ❌ Hallucination 위험 |
| **검증가능성** | ✅ 로직이 명시적 | ❌ Black Box |
| **일관성** | ✅ 동일 입력 → 동일 출력 | ❌ 매번 다른 표현 가능 |
| **비용** | ✅ 무료 (로컬 실행) | ❌ API 비용 발생 |
| **속도** | ✅ 밀리초 단위 | ❌ 초 단위 |
| **유연성** | ❌ 새 패턴 추가 시 코드 수정 | ✅ 프롬프트만 변경 |

### 핵심 이유: **통계 보도자료의 특성**

```
통계 보도자료 = 정확한 수치 + 정해진 서식 + 반복 패턴

→ 규칙기반이 최적
→ LLM은 "해설/분석" 문구에만 적합
```

**예시:**
- ✅ 규칙기반 적합: "서울의 광공업생산은 전년동분기대비 3.2% 증가"
- 🤖 LLM 적합: "이번 분기 서울의 생산 증가는 반도체 수출 호조에 기인한 것으로 분석됨"

---

## 6️⃣ 발표 시 강조 포인트

### 핵심 메시지

1. **"규칙기반 = 정확성"**
   - 통계 보도자료에서 가장 중요한 것은 **수치의 정확성**
   - 규칙기반은 데이터가 맞으면 문구도 **100% 정확**

2. **"검증 가능한 로직"**
   - 모든 변환 규칙이 **코드에 명시적으로 정의**
   - 담당자가 규칙을 이해하고 **신뢰**할 수 있음

3. **"확장 가능한 구조"**
   - 새로운 보도자료 추가 시 Generator/Template/Schema만 추가
   - 핵심 엔진은 **재사용**

### 데모 시나리오

```
[시나리오]
1. 엑셀 파일 업로드
2. "이 데이터를 보세요. '충남'의 증감률이 8.5%입니다."
3. "템플릿에서 이 조건을 확인합니다: growth_rate > 0 → '증가'"
4. "결과를 보세요: '충남(8.5%)은... 증가'"
5. "만약 값이 -2.3%라면? 자동으로 '감소'로 바뀝니다."
```

---

## 7️⃣ 구현 통계

| 항목 | 수량 |
|------|------|
| Generator 모듈 | 15개 |
| HTML 템플릿 | 71개 |
| JSON 스키마 | 39개 |
| 매핑 사전 항목 | 100+ 개 |
| 조건문 규칙 | 50+ 개 |
| 총 코드 라인 | 15,000+ 줄 |

---

## 8️⃣ 코드 예시 (발표용)

### 가장 간단한 예시

```python
# 규칙 1: 증감 방향 자동 판단
growth_direction = "증가" if growth_rate > 0 else "감소"

# 규칙 2: 지역 분류
increase_regions = [r for r in regions if r["growth_rate"] > 0]
decrease_regions = [r for r in regions if r["growth_rate"] < 0]

# 규칙 3: 순위 정렬
top3_increase = sorted(increase_regions, key=lambda x: x["growth_rate"], reverse=True)[:3]
```

```html
<!-- 규칙 4: 조건부 텍스트 생성 -->
{{ region }}의 광공업생산은 전년동분기대비 
{{ "%.1f"|format(growth_rate) }}% 
{% if growth_rate > 0 %}증가{% else %}감소{% endif %}
```

**결과:**
- 입력: `{"region": "서울", "growth_rate": 3.2}`
- 출력: "서울의 광공업생산은 전년동분기대비 3.2% 증가"

---

*마지막 업데이트: 2026년 1월 2일*

